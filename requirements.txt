numpy==2.2.3
pandas==2.2.3
# Parquet backend for pandas (sentiment_scores.parquet).
pyarrow==19.0.1
scikit-learn==1.6.1
torch==2.6.0
# Qwen3-Embedding requires transformers >= 4.51; pinned to the version in the env.
transformers==5.12.1
nltk==3.9.1
spacy==3.8.14
# spaCy Portuguese model used for TF-IDF lemmatization (03_sentiment_classifier/tfidf_lr).
# Installable via `python -m spacy download pt_core_news_sm` or the pinned wheel below.
pt-core-news-sm @ https://github.com/explosion/spacy-models/releases/download/pt_core_news_sm-3.8.0/pt_core_news_sm-3.8.0-py3-none-any.whl
zvec==0.5.0
tqdm==4.68.3
lmstudio==1.5.0
PyYAML==6.0

# 04_recommender — semantic search + re-rank + web service
sentence-transformers==5.6.0
fastapi==0.138.1
uvicorn[standard]==0.49.0
httpx==0.28.1
pytest==8.3.4
